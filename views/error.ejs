<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="error-container">
        <div class="error-content">
            <div class="error-visual">
                <div class="error-icon error">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div class="error-waves">
                    <div class="wave"></div>
                    <div class="wave"></div>
                    <div class="wave"></div>
                </div>
            </div>
            
            <div class="error-text">
                <h1>Oops!</h1>
                <h2>Something went wrong</h2>
                <p><%= message %></p>
                <p class="error-details">Our AI systems are working to resolve this issue. Please try again in a moment.</p>
            </div>
            
            <div class="error-actions">
                <a href="/" class="btn btn-primary btn-large">
                    <i class="fas fa-home"></i>
                    Go Home
                </a>
                <button onclick="location.reload()" class="btn btn-outline btn-large">
                    <i class="fas fa-redo"></i>
                    Try Again
                </button>
            </div>
        </div>
    </div>
    
    <style>
        .error-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(ellipse at center, rgba(239, 68, 68, 0.1) 0%, transparent 50%),
                        linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
        }
        
        .error-content {
            max-width: 600px;
            padding: 2rem;
        }
        
        .error-visual {
            position: relative;
            margin-bottom: 3rem;
        }
        
        .error-icon.error {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            animation: pulse 2s ease-in-out infinite;
            box-shadow: 0 20px 40px rgba(239, 68, 68, 0.3);
        }
        
        .error-icon.error i {
            font-size: 3rem;
            color: white;
        }
        
        .error-waves {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 200px;
            height: 200px;
            pointer-events: none;
        }
        
        .wave {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 100px;
            height: 100px;
            border: 2px solid rgba(239, 68, 68, 0.3);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            animation: wave 3s ease-out infinite;
        }
        
        .wave:nth-child(1) { animation-delay: 0s; }
        .wave:nth-child(2) { animation-delay: 1s; }
        .wave:nth-child(3) { animation-delay: 2s; }
        
        .error-text h1 {
            font-size: 4rem;
            font-weight: 900;
            color: #ef4444;
            margin-bottom: 1rem;
            letter-spacing: -0.05em;
        }
        
        .error-text h2 {
            font-size: 2rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }
        
        .error-text p {
            font-size: 1.1rem;
            color: var(--text-secondary);
            line-height: 1.6;
            margin-bottom: 1rem;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .error-details {
            color: var(--text-muted) !important;
            font-size: 0.95rem !important;
            margin-bottom: 3rem !important;
        }
        
        .error-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 20px 40px rgba(239, 68, 68, 0.3);
            }
            50% {
                transform: scale(1.05);
                box-shadow: 0 25px 50px rgba(239, 68, 68, 0.4);
            }
        }
        
        @keyframes wave {
            0% {
                width: 100px;
                height: 100px;
                opacity: 1;
            }
            100% {
                width: 300px;
                height: 300px;
                opacity: 0;
            }
        }
        
        @media (max-width: 768px) {
            .error-content {
                padding: 1rem;
            }
            
            .error-text h1 {
                font-size: 3rem;
            }
            
            .error-text h2 {
                font-size: 1.5rem;
            }
            
            .error-actions {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</body>
</html>